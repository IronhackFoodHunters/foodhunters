

<button class="createbuttonhomepage"> <a class="createbuttonhomepage" href="/private/create-recipe"> CREATE A RECIPE </a> </button>
<br>
<br>

    {{#each reversed}}

<section class="sectionborder">

    <div class="sectionrecipehomepage">
    <h4 class="homepagetexth4">{{title}}</h4>

    <a href="/recipe-details/{{_id}}">
        <img class="imagerecipe" src="{{imageUrl}}" width="200" height="200" alt="{{title}}" />
    </a>
    </div>

 <div class="sectiontitleownerhomepage">
    <h3 class="homepagetexth3">{{owner.username}}</h3>

    <!-- <a href="/profile/{{owner.username}}"> -->

    <img class="imagesProfile" src="{{owner.imageUrl}}" width="100" height="100" alt="{{recipe.owner.username}}">
    <h4 class="homepagetexth4"> {{recipe.owner.username}}</h4>
</div>

    <form  class="likebuttonprofile" action="/favourites/{{_id}}" method="POST">
        <button class="likeButton">LIKE</button>
        <!--when this is pushed it need to push the recipe in to recipesLiked-->
    </form>
    </section>
    <br>
    <hr class="breaklinehomepage">
    {{/each}}
   